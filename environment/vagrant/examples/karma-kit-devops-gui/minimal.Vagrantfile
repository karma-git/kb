# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
    config.vm.box = "karma-kit/focal64-devops"
    config.vm.box_version = "1.0"
    config.vm.hostname = "ubuntu-devops"
    config.vm.synced_folder "./", "/srv/"
  end
